# Optional: frontend base URL if app runs on different port, default: `/`
# frontendUrl = "http://localhost:5371"

[auth]
# Required
jwtSecret = "your-super-secret-change-me"

# Optional: enable Microsoft 365 / Entra ID login
# [auth.microsoft]
# clientId     = "<Azure app registration client ID>"
# tenantId     = "<tenant ID or 'common'>"
# clientSecret = "<client secret value>"
# redirectUri  = "https://your-domain.de/api/auth/callback/microsoft"
