{
  "name": "server-cli",
  "version": "0.0.0-dev",
  "scripts": {
    "compile": "bun build ./src/cli.ts --compile --outfile ./dist/cli.bin",
    "check": "tsgo"
  },
  "dependencies": {
    "@serkonda7/ts-result": "catalog:",
    "drizzle-orm": "catalog:",
    "server": "workspace:*",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "drizzle-kit": "catalog:"
  }
}
