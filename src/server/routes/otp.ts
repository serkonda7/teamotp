export async function handle_get_otp_entries(req: Request): Promise<Response> {
	// TODO
	return Response.json({}, { status: 200 })
}

export async function handle_add_otp_entry(req: Request): Promise<Response> {
	// TODO
	return Response.json({}, { status: 200 })
}
